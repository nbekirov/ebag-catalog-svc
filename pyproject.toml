[project]
name = "ebag-catalog"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=6.1.1",
    "djangorestframework>=3.18.1",
]

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
